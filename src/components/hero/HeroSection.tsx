export function HeroSection() {
  return (
    <section aria-labelledby="hero-heading" className="flex min-h-[80vh] flex-col items-center justify-center px-6 text-center">
      <h1 id="hero-heading" className="text-5xl font-bold tracking-tight">
        {/* Hero headline — replace with v0 component */}
        AI į jūsų gyvenimą — paprastai ir be streso
      </h1>
      <p className="mt-6 max-w-2xl text-xl text-muted-foreground">
        {/* Subheadline placeholder */}
        Padedame verslui ir žmonėms pradėti naudoti dirbtinį intelektą — žingsnis po žingsnio, be techninio jargono.
      </p>
    </section>
  );
}
